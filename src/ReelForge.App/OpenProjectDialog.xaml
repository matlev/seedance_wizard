<Window x:Class="ReelForge.App.OpenProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open a ReelForge project"
        Width="760" Height="470" MinWidth="640" MinHeight="400"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}">
    <Grid Margin="22" Background="{StaticResource WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="OPEN PROJECT" Style="{StaticResource ApplicationTextBlockStyle}"
                   FontSize="17" Margin="0,0,0,5" />
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}"
                   Text="Open a recent project, choose a ReelForge project file directly, or choose a folder containing one project." />

        <WrapPanel Grid.Row="2" Margin="0,16,0,12">
            <Button Content="Choose .rfp file..." Click="ChooseProjectFile_Click" Margin="0,0,8,0" />
            <Button Content="Choose folder..." Click="ChooseProjectFolder_Click" />
        </WrapPanel>

        <TextBlock Grid.Row="3" Text="Recent projects" Style="{StaticResource ApplicationTextBlockStyle}"
                   Margin="0,0,0,5" />
        <Grid Grid.Row="4">
            <ListBox x:Name="RecentProjectsList" SelectionChanged="RecentProjectsList_SelectionChanged"
                     MouseDoubleClick="RecentProjectsList_MouseDoubleClick"
                     Background="#10131A" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="8,6" ToolTip="{Binding ProjectFilePath}">
                            <TextBlock Text="{Binding DisplayName}" Style="{StaticResource ApplicationTextBlockStyle}"
                                       FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding ProjectFilePath}" Foreground="{StaticResource MutedTextBrush}"
                                       FontSize="11" Margin="0,3,0,0" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock x:Name="RecentProjectsEmptyText" Text="No available recent projects yet."
                       IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}" />
        </Grid>

        <TextBlock x:Name="ValidationErrorText" Grid.Row="5" TextWrapping="Wrap"
                   Foreground="#FF9B9B" FontWeight="SemiBold" Margin="0,10,0,0" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button x:Name="OpenProjectButton" Content="Open project" Click="OpenProject_Click"
                    IsDefault="True" IsEnabled="False"
                    Background="{StaticResource AccentBrush}" Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
