<Window x:Class="ReelForge.App.AssetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change asset name" Width="480" Height="230"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="CHANGE ASSET NAME" Style="{StaticResource ApplicationTextBlockStyle}" FontSize="16" />
        <TextBlock Grid.Row="1" Text="This changes the name shown in ReelForge; the stored media filename is unchanged."
                   TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,12" />
        <TextBox x:Name="AssetNameTextBox" Grid.Row="2" />
        <TextBlock x:Name="ValidationText" Grid.Row="3" Foreground="#FF9B9B" Margin="0,8,0,0"
                   Visibility="Collapsed" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button Content="Update" IsDefault="True" Background="{StaticResource AccentBrush}" Foreground="White"
                    Click="Update_Click" />
        </StackPanel>
    </Grid>
</Window>
