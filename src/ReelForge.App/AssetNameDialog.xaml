<Window x:Class="ReelForge.App.AssetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change filename" Width="480" Height="250"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="CHANGE FILENAME" Style="{StaticResource ApplicationTextBlockStyle}" FontSize="16" />
        <TextBlock Grid.Row="1" Text="Rename the stored media file. Its file type and contents stay unchanged."
                   TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,12" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FileNameStemTextBox" />
            <Border Grid.Column="1" Background="{StaticResource PanelRaisedBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" Padding="9,7" Margin="5,0,0,0">
                <TextBlock x:Name="ExtensionText" Style="{StaticResource ApplicationTextBlockStyle}" VerticalAlignment="Center" />
            </Border>
        </Grid>
        <TextBlock x:Name="ValidationText" Grid.Row="3" Foreground="#FF9B9B" Margin="0,8,0,0"
                   Visibility="Collapsed" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button Content="Rename" IsDefault="True" Background="{StaticResource AccentBrush}" Foreground="White"
                    Click="Update_Click" />
        </StackPanel>
    </Grid>
</Window>
