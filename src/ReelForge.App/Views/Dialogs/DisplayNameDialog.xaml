<Window x:Class="ReelForge.App.Views.Dialogs.DisplayNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Saved Clip" Width="480" Height="230"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="RENAME SAVED CLIP" Style="{StaticResource ApplicationTextBlockStyle}" FontSize="16" />
        <TextBlock Grid.Row="1" Text="Change the name shown in ReelForge. The clip's source and recipe stay unchanged."
                   TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,12" />
        <TextBox x:Name="DisplayNameTextBox" Grid.Row="2" />
        <TextBlock x:Name="ValidationText" Grid.Row="3" Foreground="#FF9B9B" Margin="0,8,0,0"
                   Visibility="Collapsed" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True" />
            <Button Content="Rename" IsDefault="True" Background="{StaticResource AccentBrush}" Foreground="White"
                    Click="Rename_Click" />
        </StackPanel>
    </Grid>
</Window>
