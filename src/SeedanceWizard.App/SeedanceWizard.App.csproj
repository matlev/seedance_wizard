<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <!-- UI text intentionally follows the current culture; service fields remain interface-typed for substitution. -->
    <NoWarn>$(NoWarn);CA1305;CA1859</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SeedanceWizard.Core\SeedanceWizard.Core.csproj" />
    <ProjectReference Include="..\SeedanceWizard.Application\SeedanceWizard.Application.csproj" />
    <ProjectReference Include="..\SeedanceWizard.Infrastructure\SeedanceWizard.Infrastructure.csproj" />
  </ItemGroup>

</Project>
