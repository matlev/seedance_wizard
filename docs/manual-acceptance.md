# Manual regression acceptance matrix

Use this concise smoke matrix after a change touches the named area, and run it in full before merging a milestone that affects the desktop application. It captures the durable manual regression coverage established during Milestones 2 and 3.

Automated tests remain network-isolated and must never submit a paid provider request. Any live generation check is a deliberate human-only action and is not required for refactor acceptance.

| Area | Manual acceptance path | Expected result |
| --- | --- | --- |
| Project lifecycle | Create, close, reopen, switch between two projects, then restart ReelForge. After an interrupted save leaves recovery data, restart once and open the recovered working state, then repeat and discard recovery. | The last project reopens; project-specific selection/workspace state does not leak between projects; recent projects remain usable. Recovery is explicitly offered, never auto-activated, and opening it leaves the committed project unchanged until Save; discarding it keeps the committed project and retires the candidate. |
| Project cloning | Open **File > Clone project…** with a saved current project, verify it is selected by default, then choose a different recent or browsed `.rfp`. Select a destination and clone name, confirm, observe the replacement progress window, and cancel once during a large copy. Repeat to completion and open the clone from Recent Projects. Try an existing destination and a destination inside the source project. | Cancel leaves no visible partial clone and the current project remains open. A completed clone has a new project identity/name but preserves all media, recipes, provenance, anchors, generation history, compositions, exports, and project-relative references. Cache and recovery data are absent and rebuildable; the clone reopens cleanly, the source is unchanged, and no provider request occurs. Existing, source-equal, and source-nested destinations are refused. |
| Project location | With no active generation job, open **File > Settings > Project**, choose a new parent folder, confirm the exact destination, move the active project, and restart. Repeat once to another volume if available. Try an existing destination, a path inside the project, an unsafe lifecycle state, and a move while a fake-provider job remains unreconciled. | The complete project folder moves without changing project identity or project-relative meaning; the active workspace, title, recent list, and last-project path use the destination and restart succeeds. Same-volume moves do not expose a partial copy; cross-volume publication validates before rebinding and source retirement. Invalid, linked, nested, occupied, unsafe-state, and active-job moves are refused without changing the source. No provider request occurs. |
| Project media integrity | Move one imported source out of its recorded project location, select it, and use **Relink source…** first with a different file and then with an exact copy of the original bytes. Reopen the project after the successful relink. | Missing or inaccessible media is reported without becoming usable. Mismatched bytes are refused and may only be imported as new media. The matching candidate is copied into project-relative storage, retains the same asset and logical references, and reopens as available without any provider request. |
| Dropped missing-source relink | Move one imported source out of its recorded project location and select it so it is reported missing. Drag the exact original bytes back into ReelForge, then reopen the project. Repeat with different bytes that use the missing source's filename. | The exact candidate relinks the existing asset in place: Project Media contains one item, its `AssetId` and derived references remain intact, and it reopens as available. Different bytes import as a distinct asset at a collision-free project-relative path while the original remains independently missing; two active assets never share one physical path. No provider request occurs. |
| Missing-media actions and deletion | Move one imported source out of its recorded project location and select it. Verify its Project Media indicator, tooltip, context menu, and preview actions. Use **Relink** once, then choose **Delete** for a source used by at least one Saved Frame, Saved Clip, extracted asset, or composition; accept the warning and reopen the project. | The missing item shows a yellow warning triangle. Its context menu contains only **Relink source…** and **Delete from project**; the preview contains the same two actions and no implementation-language guidance. Relink uses verified relinking. Delete remains available despite dependencies, removes the item and contained file after confirmation, preserves referenced logical history as degraded state, and survives reopen without cleanup, cache pruning, or provider activity. |
| Deleted-source restoration | Delete a referenced physical source, then import an exact copy of its original bytes by file picker or drag-and-drop. Choose the deleted identity in the restoration prompt and reopen the project. Repeat by choosing **Import as new**, then use **Restore deleted source links…** on the imported copy. Also try different bytes and cancel one prompt in a multi-file import. | Restoration is always explicit and SHA-256 verified. Restoring revives the original asset identity and its Saved Frames, Clips, Audio, compositions, provenance, and historical references; it does not silently deduplicate active media. Import-as-new creates a distinct asset. An unused imported copy may be folded into the restored identity, while a referenced copy remains distinct. Different bytes are not offered as a match, and cancelling the decision leaves the entire batch unchanged. No provider request occurs. |
| Degraded media and project cleanup | Materialize a Saved Frame, Saved Clip, and composition, then move or delete a physical source they require. Select each affected item, inspect **File > Settings > Project**, cancel Cleanup once, then confirm it and reopen the project. Repeat with an affected item that has never been cached and with a healthy extracted-audio file whose provenance names the source. | Missing physical media keeps its yellow warning; affected derived items show a red warning triangle. Their unavailable-preview message always says Cleanup Project will delete them, and only a still-valid cache entry adds the advice that it can be exported permanently. Existing Export rescues those cached bytes without rendering. Cleanup explicitly warns that missing media could otherwise be relinked, then removes only the degraded active Frames, Saved Clips, and compositions in one save while retaining hidden history; the healthy extracted audio remains. Cache contents are not pruned or reconciled, cancellation/save failure leaves active project state unchanged, reopen is valid, and no provider request occurs. |
| Degraded timing placement | Import representative Exact, Estimated, and Unusable timing sources; use `tests/ReelForge.Tests/Fixtures/degraded_timing_gap.webm` for the Estimated-video case. Also place an ordinary H.264/AAC MP4 whose AAC stream reports explicit reconciled priming metadata. Place each Exact/Estimated video/audio combination, cancel and choose **Attempt Repair** once, explicitly choose video-only when audio is Unusable, then reopen. Reinspect an Estimated source after placement without adopting it. | The supplied fixture is assessed as Estimated with a 1.333-second frozen span and a discontinuous-timestamps issue. Its WebM preview may remain unavailable through the current Windows player; preview-format fallback belongs to the later preview/proxy milestone and does not change its placement assessment. Reconciled ordinary AAC remains Exact and opens no degraded-timing prompt. Estimated placement opens its acknowledgement on the UI thread, pins the frozen span/source/stream/issues, and shows persistent yellow Project Media, occurrence, Inspector, and composition warnings. Exact and Estimated audio/video statuses remain independent. Unusable video is blocked; Unusable audio is never silently omitted. Reopen preserves geometry and synchronization; later analysis changes no occurrence until an explicit edit. Precision-dependent tools explain their missing evidence, and no repair/render/provider work starts implicitly. |
| Multitrack foundation | Start an empty Working Composition and switch between it and Project Media several times. Add at least two video and two audio tracks from the global timeline buttons, rename unlocked tracks, reorder each media-type list, select track rows, toggle video visibility, audio mute, and each lock, then save and reopen. Delete empty tracks, including the last track of one type, and recreate that type from the timeline toolbar. Try rename/visibility/mute/reorder/delete while a track is locked. Place a video with selected audio, then drop a second video near (but not exactly on) the first video's end and inspect the audio-track chooser. | The empty composition shows a normal add-media placeholder without an audition-failed modal. Ordered track rows, stable identities, custom names, explicitly empty tracks, and controls reopen exactly. Track rows do not contain redundant add buttons. Lock state uses locked/unlocked icons; video visibility uses open/closed-eye icons and neither duplicates its state as text. Video occurrences fit fully inside the taller video rows. Track bodies show a subtle repeating play-icon or music-note texture, and the audio chooser displays readable track names. Video and audio track order remain independent; selection is transient. The second video appends at the first video's exact end, linked audio preserves its pinned synchronization, and audition does not report a pointer-position overlap. If an unsupported gap or overlap exists, its message names the offending media and current millisecond-formatted timeline start instead of exposing an internal UUID. Each real command creates one committed composition revision, no-op controls create none, and locked-track mutations refuse without partial state. Empty-track deletion never deletes Project Media or cache, and a zero-track media-type list remains recoverable through the global add-track controls. Disabled primary actions remain visibly faded versions of their normal color, and no placement, render, repair, or provider work starts implicitly. |
| Exact occurrence audio Detach | Place an Exact video with usable Exact audio. Right-click its video occurrence and choose **Detach audio...**. Cancel the filename prompt once, then complete it. Reopen the project and compare the video/audio occurrence timing, item count, mix controls, and Project Media. Repeat with either affected track locked and with an Estimated-audio occurrence. | Cancellation changes nothing. The completed action creates one durable project-relative Audio asset from only the selected occurrence's exact selected stream/sample range, keeps the existing audio occurrence identity/start/mix/fades, rebinds it to the new asset, removes the link from both occurrences, and creates no second audible route. The derived asset records source and occurrence provenance and reopens with exact frozen geometry. A locked affected track or non-Exact audio occurrence disables/refuses Detach before extraction; failures leave no asset, file, or composition revision. Whole-source **Extract audio** remains a separate Project Media action. No provider request occurs. |
| Jobs and recovery | Queue a fake-provider job, switch projects, restart while it is running, then observe completion. Exercise Undo Send cancellation separately. Inspect the Jobs guidance before and after submission. | Global job state and elapsed time survive restart; finalization targets the owning project; queued work cancelled during Undo Send is never submitted. Jobs explains that Cancel Job is limited to Undo Send and that current providers do not offer remote cancellation after submission. |
| Generation preparation | Prepare text, physical-media, Saved Frame, and Saved Clip references without confirming a paid request. With AtlasCloud Seedance 2.5 selected, switch to VideoEdit, select one 4–30 second video, then try no source, an image, two videos, and videos outside that duration range. Switch back to an ordinary generation mode. | Provider capabilities constrain the form correctly; reference order/role/label/notes are preserved. Atlas VideoEdit locks duration to Source (`-1`) and ratio to `adaptive`, explains that exactly one 4–30 second source video is required, and rejects every other reference shape before submission. Ordinary modes restore flexible duration and ratio controls. No request occurs before explicit confirmation. |
| Frame and clip tools | In both Select Frame and Make Clip, navigate exact frames using the contact strip and the viewer's previous/next-frame buttons; then save/update/delete a frame, create/replay/delete a short Saved Clip, and reconstruct after clearing cache. | The contact strip and viewer remain synchronized on the same decoded PTS; viewer-button stepping remains paused and silent; derived media rebuilds from durable recipes; missing sources produce a clear failure. |
| Deferred structural timeline grammar (Milestone 5) | With multiple occurrences present, try dragging an existing video or audio occurrence, dropping new video before or between existing video occurrences, and locating split/trim/ripple/replace/snapping/marker/session-undo and **Unlink audio** commands. | ReelForge does not imply that these interactions are currently supported: existing occurrences do not move, new video placement uses the current append contract, and the listed structural commands are absent or disabled. The committed composition remains unchanged. These are planned Milestone 5 capabilities, not Milestone 4 regression failures. |
| Bounded audition foundation | Audition an empty composition and a supported contiguous single-visible-video-track composition, including linked audio. Then create a gap, overlap, or additional contributing visual layer and try again. | Empty composition selection stays quiet. The supported shape auditions without changing project state. An unsupported shape stops with actionable guidance that names the offending media and timeline position; it never flattens, omits, retimes, or renders implicitly. General multitrack audition belongs to Milestone 6. |
| Deferred composition preview/export (Milestone 6) | Inspect composition preview/export availability for a project that needs arbitrary multitrack rendering, and select the supplied degraded WebM fixture in Project Media. | ReelForge does not claim a general composition render or Windows-player WebM fallback in Milestone 4. Unsupported preview/export remains unavailable or stops with actionable guidance, and the WebM limitation does not change the persisted timing assessment. Track-aware rendering, preview/proxy fallback, and final export preflight are Milestone 6 capabilities rather than Milestone 4 regressions. |
| Settings and platform integration | Change settings rapidly, configure/browse/auto-detect each media tool, update/remove a credential, change log/cache locations, and restart. | Atomic settings remain readable; platform defaults and overrides resolve correctly; credentials never appear in JSON/logs; configuration survives restart. |
| Application chrome | Use the File and View menus with both mouse and keyboard, including every project command, workspace switch, Jobs, and Exit. Drag and double-click blank title-bar regions; resize, minimize, maximize/restore, Aero Snap, and open the system menu with Alt+Space. Repeat at 100%, 125%, and 150% display scaling, and observe the title bar while a fake-provider job is queued or running. | Menus and window controls share one title bar without obscuring the project title or workspace selectors. Existing commands retain their enablement and behavior; native move/resize/snap/system-menu behavior remains available; long project titles trim safely; and the animated blacksmith remains visible while work is active. No menu or passive chrome action submits provider work. |
| Cache and diagnostics | Hold one derived item in use while producing enough media to trigger eviction; inspect the configured verbose log. | Active leases are not evicted; disposable entries can rebuild; diagnostics identify failures without exposing credentials. |

The Windows shell remains the current manual presentation target. Screenshot-perfect automation is not required; extracted presenter/state logic should carry deterministic tests as it is introduced.
