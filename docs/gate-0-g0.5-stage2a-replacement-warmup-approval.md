# Gate 0 G0.5 Stage 2A replacement warm-up approval

Date: 2026-08-27

Status: owner-approved replacement warm-up and exact matrix restart

The owner classifies `g05-stage2a-20260827T113709087Z-9c98c3d3` as a non-authoritative pre-matrix proof-harness defect. It is not a retained Stage 2A matrix row, route result, successful warm-up, failed capability result, measured observation, or input to matrix statistics. Its local diagnostic bytes remain truthful but incomplete because no approved summary/evidence record was completed.

The narrow validator repair is approved. It may normalize only the live runner's ordered dictionary and its nested validation, hash, and cleanup dictionaries. No resume, reconstruction, or recovery subsystem is authorized.

One replacement warm-up is authorized by restarting the unchanged frozen schedule at global ordinal 1 under a fresh proof-run identity and fresh run directory beneath the approved staging root. The authoritative matrix remains exactly 18 cells, one warm-up plus five measured attempts per cell, and 108 authoritative attempts. The abandoned defect attempt may not be reused, reconstructed, spliced into the new run, or counted in any matrix disposition or statistic.

Before restart, the committed repair and real-shape regression must pass the complete 28-test focused no-media Stage 2A suite; the working tree must be clean except for generated execution evidence; and a fresh full preflight must confirm zero active FFmpeg/ffprobe processes, exact P2 identity, approved memory/free-space floors, valid local/R2 manifests, and unchanged sealed legacy/root-index identities except for previously approved infrastructure records.

The Stage 2A owner packet must report:

- 108 authoritative Stage 2A matrix attempts;
- 109 total physical media attempts since Stage 2A execution began;
- one additional non-authoritative pre-matrix proof-harness attempt excluded from every warm-up, measured, capability, and statistical count.

The existing defect closure must remain local through owner-packet review. Its record must include the run ID, failure phase, non-authoritative reason, 18 files, 27,125,317 bytes, MP4 SHA-256 `C876FE2865D6543B6FD8853E5D68968AAEEA76C627D2DC785C3E5BC836A7B5F7`, missing completed summary, missing shard/root entry, missing R2 receipt, and orphan-free process state. Its bytes count toward overall local Gate 0 storage but not authoritative Stage 2A retention or route statistics. Deletion requires later owner permission.

This approval authorizes only the existing Stage 2A matrix restart and completion packet. It does not authorize Stage 2B, concurrency, long-form work, new media investigations or fixtures, product integration, shipping-runtime selection, distribution, licensing, patent, or legal conclusions. After Stage 2A completes or blocks, stop for owner review.
